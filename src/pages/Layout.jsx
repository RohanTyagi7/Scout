import { Outlet, Link } from "react-router-dom";
import React, { useState } from "react";
import '../App.css';

const Layout = () => {
  return (
    <>
      <nav>
        <center>
           
          </center>
      </nav>

      <Outlet />
    </>
  )
};

export default Layout;